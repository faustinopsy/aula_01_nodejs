# aula_01_nodejs
aula 00 de node - backend em js

# terminal bloqueand

```
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
```

# instalar bibliotecas
```
npm install express
npm install --save-dev nodemon
npm install dotenv
```

# comando básicos
- git add .
- git commit -m "criado o primeiro servidor com o express"
- git push